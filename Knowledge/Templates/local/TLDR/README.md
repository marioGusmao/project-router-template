# Local TLDRs

Add project-specific TLDRs and feature summaries here after private bootstrap.

- This directory is `private_owned`.
- Template sync must not overwrite its contents.
- Each TLDR should be a short summary of a feature, decision, or workflow specific to this project.
- Keep template-level documentation in `Knowledge/*.md`.
- Use subdirectories if TLDRs grow beyond a handful of files.
